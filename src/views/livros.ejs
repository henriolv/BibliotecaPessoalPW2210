<<<<<<< HEAD
<!doctype html>
<html lang="pt-br">
<head>
  <meta charset="utf-8">
  <title>Biblioteca — Livros</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
  <style>
    :root{
      --accent:#25655a;
      --accent-dark:#1f4a44;
      --muted:#8f9a98;
      --card:#ffffff;
      --bg:#f6f8f7;
    }
    body{ background:var(--bg); color:#142522; font-family: Inter, "Segoe UI", Roboto, Arial, sans-serif; }
    .navbar { background: linear-gradient(90deg,var(--accent),var(--accent-dark)); }
    .card { border: 1px solid #e6efec; border-radius:10px; background:var(--card); }
    .cover { width:100%; aspect-ratio:3/4; object-fit:cover; background:#e9efee; border-top-left-radius:10px; border-top-right-radius:10px; }
    .btn-primary{ background:var(--accent); border-color:var(--accent-dark); }
    footer { background:#eef6f4; border-top:1px solid #e1ede9; padding:1.25rem 0; color:var(--muted); }
  </style>
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
      <a class="navbar-brand" href="/">🏛️ Biblioteca</a>
      <div class="d-flex gap-2">
        <a class="btn btn-outline-light btn-sm" href="/usuario/login">Entrar</a>
        <a class="btn btn-light btn-sm" href="/livros">Livros</a>
      </div>
    </div>
  </nav>

  <main class="py-5">
    <div class="container">
      <div class="d-flex align-items-center mb-4">
        <h1 class="me-3">Livros</h1>
        <small class="text-muted">Gerencie o acervo de livros</small>
        <div class="ms-auto"><a href="/" class="btn btn-sm btn-outline-secondary">Voltar ao catálogo</a></div>
      </div>

      <% if (message) { %>
        <div class="alert alert-<%= message.type === 'error' ? 'danger' : 'success' %>"><%= message.value %></div>
      <% } %>

      <div class="row g-4">
        <div class="col-12 col-lg-4">
          <div class="card p-3">
            <h5 class="mb-3">Adicionar novo livro</h5>
            <form method="post" action="/livros/add">
              <div class="mb-2">
                <label class="form-label">Título</label>
                <input name="titulo" class="form-control" required>
              </div>
              <div class="mb-2">
                <label class="form-label">Autor</label>
                <input name="autor" class="form-control" required>
              </div>
              <div class="mb-2">
                <label class="form-label">Capa (URL)</label>
                <input name="capa" class="form-control" placeholder="https://..." >
              </div>
              <div class="d-grid">
                <button class="btn btn-primary">Adicionar</button>
              </div>
            </form>
          </div>

          <div class="card p-3 mt-3">
            <h6 class="mb-2">Dicas</h6>
            <p class="text-muted small mb-0">Use títulos completos para facilitar buscas. Itens podem ser editados depois (implemente edição quando necessário).</p>
          </div>
        </div>

        <div class="col-12 col-lg-8">
          <h5 class="mb-3">Itens cadastrados</h5>

          <div class="row g-3">
            <% if (livros && livros.length) { %>
              <% livros.forEach(function(l){ %>
                <div class="col-12 col-sm-6 col-md-4">
                  <div class="card h-100 shadow-sm">
                    <img class="cover" src="<%= l.capa ? l.capa : 'https://placehold.co/400x600?text=' + encodeURIComponent(l.titulo) %>" alt="Capa">
                    <div class="card-body d-flex flex-column">
                      <div class="d-flex justify-content-between align-items-start mb-2">
                        <h6 class="card-title mb-0"><%= l.titulo %></h6>
                        <span class="badge <%= l.disponivel ? 'bg-success' : 'bg-secondary' %>"><%= l.disponivel ? 'Disponível' : 'Indisponível' %></span>
                      </div>
                      <p class="card-text text-secondary small mb-3"><%= l.autor %></p>
                      <div class="mt-auto d-flex justify-content-between align-items-center">
                        <small class="text-muted">ID: <%= l.id %></small>
                        <a class="stretched-link" href="#">Detalhes</a>
                      </div>
                    </div>
                  </div>
                </div>
              <% }) %>
            <% } else { %>
              <div class="col-12">
                <div class="alert alert-secondary">Nenhum livro cadastrado ainda.</div>
              </div>
            <% } %>
          </div>
        </div>
      </div>
    </div>
  </main>

  <footer>
    <div class="container text-center">
      <div>Biblioteca Municipal — Av. Conselheiro Antônio Prado, S/n • Centro</div>
      <div class="mt-1">Horário: Seg–Sex 9:00–18:00 • Sáb 9:00–13:00</div>
      <div class="mt-2 small">© <span id="year"></span> Biblioteca Municipal</div>
    </div>
  </footer>

  <script>
    document.getElementById('year').textContent = new Date().getFullYear();
  </script>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
</body>
=======
<!doctype html>
<html lang="pt-br">
<head>
  <meta charset="utf-8">
  <title>Biblioteca — Livros</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
  <style>
    :root{
      --accent:#25655a;
      --accent-dark:#1f4a44;
      --muted:#8f9a98;
      --card:#ffffff;
      --bg:#f6f8f7;
    }
    body{ background:var(--bg); color:#142522; font-family: Inter, "Segoe UI", Roboto, Arial, sans-serif; }
    .navbar { background: linear-gradient(90deg,var(--accent),var(--accent-dark)); }
    .card { border: 1px solid #e6efec; border-radius:10px; background:var(--card); }
    .cover { width:100%; aspect-ratio:3/4; object-fit:cover; background:#e9efee; border-top-left-radius:10px; border-top-right-radius:10px; }
    .btn-primary{ background:var(--accent); border-color:var(--accent-dark); }
    footer { background:#eef6f4; border-top:1px solid #e1ede9; padding:1.25rem 0; color:var(--muted); }
  </style>
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
      <a class="navbar-brand" href="/">🏛️ Biblioteca</a>
      <div class="d-flex gap-2">
        <a class="btn btn-outline-light btn-sm" href="/usuario/login">Entrar</a>
        <a class="btn btn-light btn-sm" href="/livros">Livros</a>
      </div>
    </div>
  </nav>

  <main class="py-5">
    <div class="container">
      <div class="d-flex align-items-center mb-4">
        <h1 class="me-3">Livros</h1>
        <small class="text-muted">Gerencie o acervo de livros</small>
        <div class="ms-auto"><a href="/" class="btn btn-sm btn-outline-secondary">Voltar ao catálogo</a></div>
      </div>

      <% if (message) { %>
        <div class="alert alert-<%= message.type === 'error' ? 'danger' : 'success' %>"><%= message.value %></div>
      <% } %>

      <div class="row g-4">
        <div class="col-12 col-lg-4">
          <div class="card p-3">
            <h5 class="mb-3">Adicionar novo livro</h5>
            <form method="post" action="/livros/add">
              <div class="mb-2">
                <label class="form-label">Título</label>
                <input name="titulo" class="form-control" required>
              </div>
              <div class="mb-2">
                <label class="form-label">Autor</label>
                <input name="autor" class="form-control" required>
              </div>
              <div class="mb-2">
                <label class="form-label">Capa (URL)</label>
                <input name="capa" class="form-control" placeholder="https://..." >
              </div>
              <div class="d-grid">
                <button class="btn btn-primary">Adicionar</button>
              </div>
            </form>
          </div>

          <div class="card p-3 mt-3">
            <h6 class="mb-2">Dicas</h6>
            <p class="text-muted small mb-0">Use títulos completos para facilitar buscas. Itens podem ser editados depois (implemente edição quando necessário).</p>
          </div>
        </div>

        <div class="col-12 col-lg-8">
          <h5 class="mb-3">Itens cadastrados</h5>

          <div class="row g-3">
            <% if (livros && livros.length) { %>
              <% livros.forEach(function(l){ %>
                <div class="col-12 col-sm-6 col-md-4">
                  <div class="card h-100 shadow-sm">
                    <img class="cover" src="<%= l.capa ? l.capa : 'https://placehold.co/400x600?text=' + encodeURIComponent(l.titulo) %>" alt="Capa">
                    <div class="card-body d-flex flex-column">
                      <div class="d-flex justify-content-between align-items-start mb-2">
                        <h6 class="card-title mb-0"><%= l.titulo %></h6>
                        <span class="badge <%= l.disponivel ? 'bg-success' : 'bg-secondary' %>"><%= l.disponivel ? 'Disponível' : 'Indisponível' %></span>
                      </div>
                      <p class="card-text text-secondary small mb-3"><%= l.autor %></p>
                      <div class="mt-auto d-flex justify-content-between align-items-center">
                        <small class="text-muted">ID: <%= l.id %></small>
                        <a class="stretched-link" href="#">Detalhes</a>
                      </div>
                    </div>
                  </div>
                </div>
              <% }) %>
            <% } else { %>
              <div class="col-12">
                <div class="alert alert-secondary">Nenhum livro cadastrado ainda.</div>
              </div>
            <% } %>
          </div>
        </div>
      </div>
    </div>
  </main>

  <footer>
    <div class="container text-center">
      <div>Biblioteca Municipal — Av. Conselheiro Antônio Prado, S/n • Centro</div>
      <div class="mt-1">Horário: Seg–Sex 9:00–18:00 • Sáb 9:00–13:00</div>
      <div class="mt-2 small">© <span id="year"></span> Biblioteca Municipal</div>
    </div>
  </footer>

  <script>
    document.getElementById('year').textContent = new Date().getFullYear();
  </script>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
</body>
>>>>>>> 8f144eb (Atualização do projeto (user/admin e controller))
</html>