<!doctype html>
<html lang="pt-br">
<head>
  <meta charset="utf-8">
  <title>Biblioteca — Séries</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
  <style>
    :root{ --accent:#25655a; --accent-dark:#1f4a44; --muted:#8f9a98; --card:#fff; --bg:#f6f8f7; }
    body{ background:var(--bg); color:#142522; font-family: Inter, "Segoe UI", Roboto, Arial, sans-serif; }
    .navbar { background: linear-gradient(90deg,var(--accent),var(--accent-dark)); }
    .card { border:1px solid #e6efec; border-radius:10px; }
    .cover { width:100%; aspect-ratio:3/4; object-fit:cover; background:#e9efee; border-top-left-radius:10px; border-top-right-radius:10px; }
    .btn-primary{ background:var(--accent); border-color:var(--accent-dark); }
  </style>
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
      <a class="navbar-brand" href="/">🏛️ Biblioteca</a>
      <div class="d-flex gap-2">
        <a class="btn btn-outline-light btn-sm" href="/usuario/login">Entrar</a>
        <a class="btn btn-light btn-sm" href="/series">Séries</a>
      </div>
    </div>
  </nav>

  <main class="py-5">
    <div class="container">
      <div class="d-flex align-items-center mb-4">
        <h1 class="me-3">Séries</h1>
        <small class="text-muted">Gerencie o acervo de séries</small>
        <div class="ms-auto"><a href="/" class="btn btn-sm btn-outline-secondary">Voltar ao catálogo</a></div>
      </div>

      <% if (message) { %>
        <div class="alert alert-<%= message.type === 'error' ? 'danger' : 'success' %>"><%= message.value %></div>
      <% } %>

      <div class="row g-4">
        <div class="col-12 col-lg-4">
          <div class="card p-3">
            <h5 class="mb-3">Adicionar nova série</h5>
            <form method="post" action="/series/add">
              <div class="mb-2">
                <label class="form-label">Título</label>
                <input name="titulo" class="form-control" required>
              </div>
              <div class="mb-2">
                <label class="form-label">Criador</label>
                <input name="criador" class="form-control" required>
              </div>
              <div class="mb-2">
                <label class="form-label">Temporadas</label>
                <input name="temporadas" type="number" class="form-control">
              </div>
              <div class="mb-2">
                <label class="form-label">Capa (URL)</label>
                <input name="capa" class="form-control" placeholder="https://..." >
              </div>
              <div class="d-grid">
                <button class="btn btn-primary">Adicionar</button>
              </div>
            </form>
          </div>
        </div>

        <div class="col-12 col-lg-8">
          <h5 class="mb-3">Itens cadastrados</h5>
          <div class="row g-3">
            <% if (series && series.length) { %>
              <% series.forEach(function(s){ %>
                <div class="col-12 col-sm-6 col-md-4">
                  <div class="card h-100 shadow-sm">
                    <img class="cover" src="<%= s.capa ? s.capa : 'https://placehold.co/400x600?text=' + encodeURIComponent(s.titulo) %>" alt="Capa">
                    <div class="card-body d-flex flex-column">
                      <div class="d-flex justify-content-between align-items-start mb-2">
                        <h6 class="card-title mb-0"><%= s.titulo %></h6>
                        <span class="badge bg-info">Série</span>
                      </div>
                      <p class="card-text text-secondary small mb-3"><%= s.criador %> <% if (s.temporadas) { %>• <%= s.temporadas %> temp.<% } %></p>
                      <div class="mt-auto d-flex justify-content-between align-items-center">
                        <small class="text-muted">ID: <%= s.id %></small>
                        <a class="stretched-link" href="#">Detalhes</a>
                      </div>
                    </div>
                  </div>
                </div>
              <% }) %>
            <% } else { %>
              <div class="col-12"><div class="alert alert-secondary">Nenhuma série cadastrada ainda.</div></div>
            <% } %>
          </div>
        </div>
      </div>
    </div>
  </main>

  <footer>
    <div class="container text-center">
      <div>Biblioteca Municipal — Av. Conselheiro Antônio Prado, S/n • Centro</div>
      <div class="mt-1">Horário: Seg–Sex 9:00–18:00 • Sáb 9:00–13:00</div>
      <div class="mt-2 small">© <span id="year"></span> Biblioteca Municipal</div>
    </div>
  </footer>

  <script>document.getElementById('year').textContent = new Date().getFullYear();</script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
</body>
</html>